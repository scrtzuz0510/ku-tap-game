# assets 폴더

이 폴더는 쿠 탭게임 ver2의 추가 에셋 파일을 위한 공간입니다.

현재 게임은 외부 이미지 파일 없이 CSS와 HTML로 캐릭터를 구현합니다.

## 추가 가능한 에셋

- 쿠 캐릭터 이미지 (PNG, SVG)
- 배경 이미지
- 효과음 파일 (MP3, OGG)
- 아이콘 파일

## 사용 방법

`index.html` 또는 `app.js`에서 `./assets/` 경로로 참조하세요.
